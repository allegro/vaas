Setup
-----
Install requirements:

    pip install mkdocs==0.12.2

Test the docs
-------------
To test the docs run:

    mkdocs serve

Build the docs
--------------
To build the docs run:

    mkdocs build
